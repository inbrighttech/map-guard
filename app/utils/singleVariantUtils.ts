// Utility functions for fetching a single variant

/**
 * Fetches a single variant by variant ID.
 */
export async function fetchVariant(variantId: string) {
  // TODO: Replace with actual API call
  // Example: return mock data
  return {
    variant: null, // Variant object
  };
}
