// Utility functions for fetching a single product and its variants

/**
 * Fetches a single product and its variants by product ID.
 */
export async function fetchProductWithVariants(productId: string) {
  // TODO: Replace with actual API call
  // Example: return mock data
  return {
    product: null, // Product object
    variants: [], // Array of variant objects
  };
}
